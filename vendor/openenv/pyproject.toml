[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv"
version = "0.2.0"
description = "Local compatibility shim for OpenEnv metadata validation."
requires-python = ">=3.11"
